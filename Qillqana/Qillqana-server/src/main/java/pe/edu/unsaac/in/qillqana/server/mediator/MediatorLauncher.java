package pe.edu.unsaac.in.qillqana.server.mediator;


/**
 * TODO This class is created as a Test Launcher for the mediator pattern, it's
 * necesary to remove this for
 * 
 * @author alexove
 */
public class MediatorLauncher {
	// private static final Logger
	// logger=Logger.getLogger(MediatorLauncher.class.getName());
	public static void main(String args[]) {
		// logger.info("Inicializando el mediador");
		// SessionMediator mediator=new SessionMediator(null,null);
		// logger.info("Agregando colegas al mediador");
		// StudentSession colleague01=new StudentSession(mediator);
		// StudentSession colleague02=new StudentSession(mediator);
		//
		// mediator.addColleague(colleague01);
		// mediator.addColleague(colleague02);
		//
		// logger.info("los colegas estan enviando mensajes");
		// colleague01.send("Hello World");
		// colleague02.send("Bye bye world");
	}
}